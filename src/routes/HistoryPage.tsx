import React, { useEffect, useState } from 'react';
import {
    View,
    StyleSheet,
    Text,
} from 'react-native';

interface propsInterface {
    
}

const HistoryPage: React.FC<propsInterface> = ({}) => {

    return (
        <View style={{ flex: 1 }}>
          <Text>HistoryPage</Text>
        </View>
    );
};

export default HistoryPage;

const styles = StyleSheet.create({
    
});